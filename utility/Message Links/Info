The two CC's below are essentially the same thing, but one contains a "pin message" feature which acts as a customized starboard of sorts in which users with certain roles can append posts to a set channel.
